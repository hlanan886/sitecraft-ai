# 项目总览

> 每次新工作会话默认只读取本文件。首次维护时补充真实信息；路线变化后立即同步。
> 本目录不得记录密钥、令牌、完整个人信息或未脱敏工具输出。

## 基本信息

- 项目名称：sitecraft-ai（一句话建站）
- 项目 ID：sitecraft-ai
- 项目负责人：用户（ZhuanZ）
- 风险等级：中（依赖第三方开源模板 + DeepSeek API + 本地代理网络）
- 当前阶段：功能迭代
- 当前状态：实施中
- 最后更新：2026-09-09

## 项目目标

- 核心：用户用自然语言一句话描述业务，AI 理解意图 → 匹配真实开源模板 → 生成整站初稿 → 工作台精修。
- 当前迭代目标：AI 建站在有限时间内稳定交付，并通过结构化终态记录持续观测交付率、耗时、部分交付、模板降级与超时。

## 范围

### 包含

- 22 个开源模板库（vendor 子模块，全部构建本地快照，preview 永久稳定）
- 一句话意图理解（行业/受众/语气/板块）+ 模板匹配
- 生成确认页（推荐模板 + 跨分类下拉）——**色系选择器已于 2026-09-09 移除**（配色一律取模板自带值）
- 工作台 / 发布页渲染真实开源模板 iframe（**2026-09-09 起不再降级为本地近似渲染**）

### 非目标

- 不做纯从零生成（Bolt 路线）——只做模板结构 + AI 内容填充
- 不做任意 HTML/CSS 自由生成——只走白名单结构化操作 + design tokens

## 技术路线与关键约束

- 技术栈：Next.js App Router + TypeScript + zod + SSE + node:test
- 模板渲染：`OpenSourceTemplateFrame` iframe 加载真实开源模板本地快照；**降级路径已移除**（原 `SiteRenderer` 兜底于 2026-09-09 删除调用点，组件待清理）
- 模板匹配：LLM 产出 `recommendedTemplateId` → `resolveTemplate`（关键词定 category → 白名单校验 → 默认兜底）
- 设计变量：**配色不再由用户选择**（2026-09-09）；`designTokens` 仍存在并覆盖模板字体/圆角/间距，属待修项 F-021
- 网络约束：npm 装依赖走国内镜像（registry.npmmirror.com）；上游 demo 不可达时靠本地快照

## 数据与安全边界

- 数据分类：站点草稿（本地存储）、生成记录（API 存证）
- 敏感信息处理：只记录脱敏摘要、证据 ID 和受控位置，不粘贴原始敏感数据。管理文档不存密钥（DeepSeek API Key 在 .env.local，已 gitignore）。

## 当前焦点

- 下一里程碑：修复产品外壳真实性问题（F-020 首页/设置页）与模板样式保真（F-021）
- 当前工作重点：门禁真实性已修复（F-018）；产品审计发现「演示层 vs 真实层」错位为主要「demo 感」来源
- 主要阻塞：workspace 消息级撤销 2 个场景测试失败（待归因）；P3.1–P3.3 代码与测试尚未纳入版本控制

## 按需读取索引

| 当前任务 | 追加读取 |
|---|---|
| 规划、实施、阻塞处理 | `PROJECT_PROGRESS.md` |
| 新增、修改、删除功能 | `PROJECT_FEATURES.md`；实施时同时读进度 |
| 版本号、发布、升级、兼容性 | `PROJECT_VERSIONS.md` |
| 测试、交付、完成声明 | `PROJECT_ACCEPTANCE.md` |
| 跨领域路线变更或一致性审计 | 全部文件 |

## 路线变更记录

按时间倒序追加：决定 ID、日期、决定、原因、影响、证据 ID、确认来源和复审条件。

- 2026-09-09 / D-003：**计划文件存放改为项目内**（`docs/plans/`），不再写 C 盘用户目录；C 盘历史计划已删除。原因：跨会话/跨机器不可追溯，且不在版本控制内。影响：项目计划唯一真相源为 `docs/plans/2026-09-09-quality-plan.md`。证据：全局 CLAUDE.md 新增「计划文件存放」段。确认来源：用户明确指令。
- 2026-09-09 / D-002：**移除色系选择，配色一律取模板自带值**；同时移除预览降级为本地近似渲染的路径。原因：实测色系仅 13/22 模板生效（9 个为 Tailwind 硬编码色），且覆盖模板配色削弱「真实开源模板」卖点；降级由「缺槽位即判预览失败」误触发。影响：删除 12 色板与生成页下拉、3 处 `SiteRenderer` 调用；新增 F-018/F-019。证据：E-025。确认来源：用户确认「删除降级」「色系选择对产品没作用」。
- 2026-09-02 / D-001：建站可靠性从“单次生成是否成功”升级为“每次执行恰好一个结构化终态 + 可计算健康指标”。原因：无法观测的卡死不能被持续治理。影响：新增 F-009 与 A-008；不改变模板白名单和结构化生成路线。证据：E-009/E-010。确认来源：用户要求从第一性原理解决并开始测试。
