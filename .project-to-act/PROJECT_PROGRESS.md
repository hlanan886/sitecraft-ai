# 项目进度

> 记录当前执行状态与有效工作节点；普通查看、搜索和无状态变化的命令不写入。

## 当前任务

| 任务 | 状态 | 负责人 | 完成条件 | 证据 ID | 最后更新 |
|---|---|---|---|---|---|
| 修复模板推荐不覆盖真实模板（F-001） | 已完成 | 用户+AI | 意图 API 多类业务推荐不同真实模板 | 7 场景 → 6 模板实测 | 2026-09-01 |
| need_info 追问轮推荐不丢失（F-002） | 已完成 | 用户+AI | 追问补全后推荐保留 | 闭环实测（screwfast 保留） | 2026-09-01 |
| 修复生成无限卡死并支持部分交付（F-006） | 已完成 | 用户+AI | 单板块异常不阻塞整站，成功内容保存并进入明确终态 | E-006、E-008 | 2026-09-02 |
| 真实板块流式状态与动态框图（F-007） | 已完成 | 用户+AI | 桌面/移动端展示等待、生成、恢复、完成、失败 | E-007 | 2026-09-02 |
| 模板兼容降级与刷新恢复（F-008） | 已完成 | 用户+AI | 显式返回模板切换原因；刷新复用服务端草稿和站点 | E-006、E-007 | 2026-09-02 |
| 建站终态观测与健康指标（F-009） | 已完成 | 用户+AI | 每次执行恰好一个结构化终态；设置页展示可计算质量指标 | E-009、E-010 | 2026-09-02 |
| 自然语言建站体验与工作台精修可靠性闭环（F-010） | 已完成 | 用户+AI | FAQ、推荐、初稿预览、真实进度、有限可取消终态、模板稳定与工作台冲突接管全部通过确定性验收 | E-011、E-012 | 2026-09-02 |
| 真实 provider 慢请求分级与端到端提速（F-011） | 进行中 | 用户+AI | 45 秒不再提前终止仍可能成功的任务；更晚硬截止保持有限终态；真实样本达到约 30 秒正常目标并形成时延与交付率证据 | E-013、待补 | 2026-09-02 |
| SiteCraft AI 全链路局部优化（Task 0–12） | 进行中 | 用户+AI | 按综合优化计划逐批完成输入事实、模板匹配、生成质量、对话一致性、发布回滚、权限、观测和最终对抗验收 | E-014、E-015、E-016、E-017、待补 | 2026-09-03 |
| 请求幂等与 SSE 重放防重复提交（Task 5） | 进行中 | 用户+AI | chat/generate 同 scope+key 不重复执行；进行中请求可识别；已完成请求重放原始 done；早退不遗留永久占位；真实 API/多标签验收补齐 | E-018、E-019、待补 | 2026-09-03 |
| 草稿发布快照、版本历史与回滚（Task 6） | 已完成 | 用户+AI | 质量门阻断、revision 冲突保护、immutable release、版本递增、幂等重放、公开页 iframe 和历史回滚均有真实 API/Playwright 证据 | E-020 | 2026-09-03 |
| workspace/actor 请求上下文与权限隔离（Task 7） | 已完成（strict flag） | 用户+AI | 私有 API 统一门禁；strict 模式下缺上下文 401、跨 workspace 403、viewer 写操作 403；公开 siteKey 保持匿名 | E-021 | 2026-09-03 |
| 生成 provenance 与 prompt 版本登记（Task 8） | 进行中 | 用户+AI | 生成/对话链路记录 provider、model、prompt 版本指纹、manifest/template/build revision 和脱敏输入哈希；真实 PostgreSQL 与 chat 运行时可追溯 | E-022（基础） | 2026-09-03 |
| 门禁真实性修复与降级移除（F-018） | 已完成 | 用户+AI | coverage-scan 有真断言且进默认回归；发布链路接入资产门禁；渲染事实接入前端；降级渲染移除；A10 中文长度误判修正 | E-025 | 2026-09-09 |
| 色系选择移除（F-019） | 已完成 | 用户+AI | 删除 12 色板与生成页色系下拉；配色一律取模板自带值 | E-025 | 2026-09-09 |
| 产品外壳真实性修复（F-020） | 已完成 | 用户+AI | 首页读真实站点列表、删假统计；设置页死按钮接线或删除 | 代码核实（2026-09-11） | 2026-09-10 |
| 模板样式保真（F-021） | 已完成 | 用户+AI | designTokens 一律置 null，模板字体/圆角/间距不再被覆盖 | 代码核实（2026-09-11） | 2026-09-10 |
| 死代码清理（F-022） | 已完成 | 用户+AI | SiteRenderer/colorTone/self-eval/supportConfig/structure-check 处置 | 见下方 2026-09-11 记录 | 2026-09-11 |

## 阻塞项

| 阻塞 | 影响 | 解除条件 | 状态 |
|---|---|---|---|
| 45 秒硬截止早于真实 provider 的可用结果 | 真实建站被提前终止，用户仍无法完成建站 | 将 45 秒改为慢请求告警与恢复点，设置更晚硬截止，并用真实事件时间线定位耗时 | 处理中 |
| workspace 消息级撤销 2 个场景测试失败 | 无法判定是既有 bug 还是未提交改动引入 | 归因后修复或修正断言；本轮 workspace 改动仅新增 `structureNotice`，未触及撤销链路 | 待归因 |
| P3.1/P3.2/P3.3 代码与测试从未 git add | 相关测试不在 `npm test` 范围内，看板却标已完成 | 纳入版本控制后重跑 | **已解除**（2026-09-12 基线提交 67f943f..71ba0f2） |

## 下一步

0. **🔴 `add_card` 容量门在生产上恒不生效**（P0，2026-09-12 真机+代码双证）：
   生产传入的 `presentation[].slot` 是**裸段名** `"features"`（`site-generator.ts:402` 与 `:778`），
   而校验器按 `${section}.items` 查（`site-operations.ts:778`/`:795`）→ `find` 永远 undefined
   → `overCapacity` 恒为空 → **越界从不被拦**。单测用的是 `"features.items"`
   （`tests/site-operations.test.ts:323`/`:364`），**测的是生产永远不会传的形态**。
   这正是 2026-09-10「AI 按容量提示写、越界到应用层炸掉整批」能发生的原因——
   当时加的防线**其实没接上**。附带 `capacitySections` 不含 `faq`，与 `cardSections` 不一致。
1. **`products` 契约两层不一致**（P0，2026-09-11 新发现）：`slots` 层 22/22 声明 `products` 且 `required:true`，`presentation` 层只有 17/22 声明。生成端写不出商品 + 逃生口 `optionalTargets` 零调用 → 新站产品区被静默隐藏（真机 3/3）。修法：给缺的 5 个模板传 `optionalTargets: ["products"]`。
2. **`/export/[siteId]` 无入口**（P0）：页面存在但全站零链接，用户拿不到可交付成品。
3. **文案长度实测**（P0）：`copyLengthLimit` 的 15/40 是拍的，卡住"一句话建站"的表达力。需按模板×槽位做字数梯度渲染实测真实破版点。
4. **F1 演示文案泄漏未硬拦**：`publish/route.ts:99` 资产 demo 仅 warning。
5. 补齐 Task 5 的 chat/execute 真实 API、多标签并发、重复 commit 计数和取消/超时后的 TTL 验收。
6. 完成 Task 8 运行时闭环：验证 PostgreSQL provenance 写入、chat 修改的 provenance 记录和旧表迁移。
7. 在不牺牲部分交付的前提下继续处理 F-011。

## 进度历史

按时间倒序追加：日期、完成事项、证据 ID、遗留问题、下一步和确认来源。不要覆盖旧记录。

- 2026-09-11：**技术债收敛批次 A（清场 + 止血）**——四路扫描 + 逐条一手核实，修复 3 项 P0 安全/数据问题，处置 2 项死代码。① **P-0 静默数据丢失**（最严重）：写入路径 `commitOperations` 从不跑 `siteDraftSchema`，而读取路径 `normalizeDraft` 解析失败会把整站**回退成 Forge 演示文案**且无报错。实测：`industry` 写 130 字 → 写入成功 → 下次读取用户的企业简介与优势标题**全部消失**。修法：新建 `lib/draft-field-limits.ts` 作为字段上限唯一来源（`siteDraftSchema` 引用它），`checkCopyLength` 扩展为写入前拦截，`site-store.ts` 四个写入点补 `assertDraftWritable` 兜底。② **P-1 默认无鉴权**：`configuredMode()` 此前 strict 之外一律 relaxed → 生产忘记设环境变量时 `curl /api/sites` 不带任何头即读到全部站点。改为非 development 一律 strict（本地 dev 仍零配置），e2e 显式设 relaxed。③ **P-2 SVG 存储型 XSS**：上传白名单移除 `image/svg+xml`，读取接口补 `X-Content-Type-Options` + `CSP: default-src 'none'`。④ **F-022 死代码清理**：删 `components/site-renderer.tsx`（353 行零引用）、`supportConfig`（schema 有默认有但零读零写，且是 P-0 回退的失败面之一）、**整条 selfEval 空管线**（`lib/ai-self-eval.ts` + 4 个硬编码 `[]` + 路由下发 + 前端零消费；结论是"删而非接"，将来做渲染回检环时另建在产物层）。⑤ **E-2 误判纠正**：`design-variants.ts` 曾被扫描标为死代码，核实后发现 `app/generate/page.tsx:1201` 真实渲染其 `adjustments`，**未删**。⑥ 账本校正：F-020/F-021 代码早已完成但账本仍标"已规划"，已更正。证据：`tests/draft-field-limits.test.ts`（4/4）、`tests/security-baseline.test.ts`（2/2）、真实 API 422 验证、生产模式 401 验证。单测 511 → 522（另 1 项 `composeTemplate` 失败属另一会话未提交的在建模块）。遗留：`products` 契约错配、`/export` 入口、15/40 实测、F1 硬拦；文档账本曾系统性滞后于代码，**规划前必须先核实代码实况**。
- 2026-09-09：门禁真实性修复 + 降级移除（F-018/F-019）——① `coverage-scan.spec.ts` 原为**零断言**且被 `playwright.config.ts` 的 `testIgnore` 排除，等于模板门禁不存在；现断言「必需槽全覆盖 + 零 demo 残留」并移出排除名单，22/22 通过。② 修 `applyDraft` 只取第一份 `sitecraft:applied` 的竞态（bridge 有 500/1500/3500/6000ms 四次重发），改为等「缺失最少」的稳定报告。③ 新增 `lib/publish-gates.ts` + 5 项单测，把资产门禁接入发布链路（此前 `isHeroAssetDemo` 是死数据）。④ `generatedContentSections` 此前零消费者，现接入工作台结构提示。⑤ 移除生成页 2 处 + 发布页 1 处 `SiteRenderer` 降级调用。⑥ 修正 A10 中文长度误判：`Math.min(汉字数, 英文词数/0.9)` 单位混用把标题上限压到 11 字（提示词写 15），导致 AI 按提示词写就被判超长、发布被拦。⑦ 移除色系选择（12 色板 + 下拉），配色一律取模板自带值。证据：E-025。遗留：workspace 2 个撤销测试失败待归因；产品外壳真实性问题（F-020）与模板样式保真（F-021）**实际已完成，账本过期**（2026-09-11 核实）。
- 2026-09-03：Task 3 完成——模板能力摘要接入草稿 provider、生成/恢复编排和局部重生成；新增内容质量门，生成 SSE `done` 返回质量报告，生成页显示评分与缺口并进入人工复核而不阻塞工作台。质量门 2/2、全量单测 238/238、生产构建 14 个页面和类型检查退出 0。证据：E-017。遗留：发布 API 尚未把 `publishable` 作为硬门禁，按 Task 6 处理。
- 2026-09-03：Task 5 幂等接线完成确定性部分——新增进程内 TTL 幂等存储、早退释放、SSE 终态重放；chat/generate 使用独立 scope，生成页与工作台为 AI 请求发送 key，并在 409 时优先展示服务端错误。定向幂等/SSE/契约测试 10/10，随后全量单测 252/252、类型检查和提升权限生产构建退出 0，差异检查无错误。证据：E-018。遗留：当前仅进程内单实例；真实 API、多标签、跨实例和取消后恢复仍待验收，不能据此宣称发布就绪。
- 2026-09-03：Task 5 真实 analyze smoke——本地服务健康 200，首次 generate analyze 200（约 10.6s），同 key 重放 200（约 39ms），重放解析为原始 `done` 并保留 request/task 元数据。未再次触发 chat/真实多标签并发，功能仍进行中。证据：E-019。
- 2026-09-03：Task 2 完成——模板画像、推荐解释、manifest 槽位元数据和能力摘要通过全量单测 235/235；生产构建 14 个页面退出 0；类型检查退出 0。构建首次发现 `lib/template-catalog.ts` 的 Locale 推断错误，已用局部 `satisfies Locale[]` 修复并复验通过。能力摘要随后在 Task 3 接入草稿 provider。证据：E-016。
- 2026-09-02：用户真实测试在 45 秒硬截止处仍未得到可用网站，说明本地故障注入验证只证明“不会无限等待”，没有证明该阈值适配真实 provider。F-010 的有限、可取消、明确终态能力仍保留；新增 F-011，下一步把 45 秒改为慢请求告警点、设置更晚硬截止并继续做端到端提速。证据：E-013。
- 2026-09-02：F-010 完成——FAQ 自动嵌入联系板块，推荐候选按业务稳定轮换，真实预览读取 10 分钟内同源已填草稿；客户端共享 45 秒绝对截止与取消，服务端保持 25 秒主截止、8 秒并发恢复和 `done` 终态；工作台确认请求接管 revision 冲突；NEXT LANDING 与 SHADCN PRO 的静态资源、样式和 wrapper 高度链通过浏览器验证。证据：E-011/E-012。遗留：未向真实 DeepSeek 外发测试内容，需用户明确授权后积累真实样本。
- 2026-09-02：F-009 完成——完整建站与局部重生成分开统计，每次执行由单终态门闩写入结构化结果；设置页展示最近 200 次完整建站的六项健康指标。观测 E2E 2/2、类型检查、生产构建和差异检查通过。证据：E-009/E-010。遗留：需真实模型样本校准时间预算。
- 2026-09-02：F-006/F-007/F-008 完成——149 项单测、类型检查、生产构建通过；生成流程 E2E 15/15，通过后动态框图聚焦复验 2/2；桌面/移动端截图无重叠或横向溢出。证据：E-006/E-007/E-008。遗留：真实模型耗时需上线观测。
- 2026-09-01：F-001/F-002 完成——prompt 强制 need_info 也推荐模板 + 模板描述增强 + 前端 need_info 种入 intent 基线。实测 7 场景推荐 6 个不同模板（atlas/screwfast/powerai/kindred/landwind/astrofy），追问补全闭环推荐保留（screwfast）。回归：142 测试全过。证据：test-flow 系列实测 + npm test。
- 2026-09-01：22 模板全部构建本地快照（F-003 完成），模板列表页/预览页 iframe 化（F-004 完成），模板库扩至 22（F-005 完成）。证据：全量 preview 实测 22×200+local；Playwright 列表页 22 iframe。
- 2026-09-01：发现 F-001/F-002 根因——意图 API 信息不足时模型返回 need_info 且前端不保存已识别的 recommendedTemplateId，追问轮推荐丢失；确认页下拉已含全部模板但匹配仍偏默认骨架。开始修复。
- 2026-09-02：F-006 开始——确认根因是首屏批 A 与板块批 B 通过无外层截止时间的 `Promise.all` 汇合；新增“about 永不返回、兄弟板块仍应完成”回归测试，19 项原测试通过且新增测试按预期失败。
- 2026-09-03：Task 0 完成——确认 `.env.local`、依赖、PostgreSQL 和 Chromium 前置状态；新增脱敏对抗输入夹具和契约测试；Playwright 改为不复用旧服务；`npm test` 226/226、`npm run build` 和 `npm run typecheck` 退出 0。证据：E-014。下一步：进入 Task 1。
- 2026-09-03：Task 6 完成——新增发布质量门、immutable release 文件/PG 存储、版本历史、公开页 release 读取和回滚 API；修复真实公开页 iframe 首次瞬时缺槽位导致过早降级的问题；PostgreSQL 发布/回滚改为站点行锁 + 单事务，回滚目标在同一事务内读取。定向 release 单测 5/5；Playwright 发布/回滚 3/3，覆盖草稿编辑不影响已发布快照、质量门阻断、revision 冲突、版本递增、回滚和幂等重放；截图 `test-results/steps/chromium-publish-rollback-desktop.png`。证据：E-020。遗留：PostgreSQL 真实并发压测尚未执行。
- 2026-09-03：Task 7 完成（strict flag）——新增 `lib/request-context.ts`，所有私有站点、发布、询盘和生成记录 API 统一执行授权；新增路由接线契约。权限/隔离定向测试 8/8，strict-mode Playwright 1/1：无上下文 401、跨 workspace 403、viewer 写操作 403、editor 读取成功、公开 siteKey 404/匿名路径保持可用。证据：E-021。遗留：当前 strict 模式依赖受控 headers，尚未接入真实内部网关签名；默认 relaxed 模式保留内部开发兼容性。
- 2026-09-03：确定性验收收尾——生成硬截止补充 `reader.cancel()`，已填预览标签可独立定位；确认页、模板列表、板块进度和局部重生成验收脚本与当前 UI/22 模板目录对齐。全量单测 288/288、定向 Playwright 7/7、类型检查、生产构建和差异检查通过。证据：E-024。遗留：消息级撤销两个场景的 helper/服务存储隔离、真实 provider 性能、多实例幂等与最终对抗审查未完成，不能宣称 ready。
